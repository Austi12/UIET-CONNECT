# Model Configuration
MODEL_NAME="resnet50"  # Options: resnet50, resnet18, mobilenet
EMBEDDING_DIM=2048     # Must match model output size
MODEL_PATH="./models"

# Matching Configuration
SIMILARITY_THRESHOLD=0.75  # Minimum similarity score (0-1)
TOP_K_MATCHES=5           # Number of top matches to return

# Server Configuration
HOST="0.0.0.0"
PORT=8000

# Database (optional - if storing embeddings directly)
# DATABASE_URL="postgresql://user:password@localhost:5432/uiet_connect"
